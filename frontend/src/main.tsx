import {StrictMode} from 'react';import {createRoot} from 'react-dom/client';import {BrowserRouter,Navigate,Outlet,Route,Routes} from 'react-router-dom';import AppHeader from './components/AppHeader';import {AuthProvider,useAuth} from './contexts/AuthContext';import {ThemeProvider} from './contexts/ThemeContext';import AuthPage from './pages/AuthPage';import Dashboard from './pages/Dashboard';import Problems from './pages/Problems';import Settings from './pages/Settings';import Revisions from './pages/Revisions';import RecentProblems from './components/RecentProblems';import './styles.css';
function Protected(){const {user,loading}=useAuth();if(loading)return <p>Loading…</p>;return user?<><AppHeader/><Outlet/></>:<Navigate to="/login" replace/>}function Dash(){const {user,loading}=useAuth();if(loading)return <p>Loading…</p>;return user?<><Dashboard/><RecentProblems/></>:<Navigate to="/login" replace/>}function App(){return <Routes><Route path="/login" element={<AuthPage/>}/><Route path="/register" element={<AuthPage register/>}/><Route element={<Protected/>}><Route path="/dashboard" element={<Dash/>}/><Route path="/problems" element={<Problems/>}/><Route path="/revisions" element={<Revisions/>}/><Route path="/settings" element={<Settings/>}/></Route><Route path="*" element={<Navigate to="/dashboard" replace/>}/></Routes>}createRoot(document.getElementById('root')!).render(<StrictMode><BrowserRouter><ThemeProvider><AuthProvider><App/></AuthProvider></ThemeProvider></BrowserRouter></StrictMode>);
