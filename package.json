{
  "name": "dsa-tracker",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["frontend", "backend", "extension"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "node scripts/build-all.cjs",
    "test": "npm run test --workspace backend && npm run test --workspace extension"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
